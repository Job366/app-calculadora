import React from "react";

const Input = () => {
    
  const onChangeValue = function (evento) {
    console.log(evento.target.value);
  };

  return (
    <div>
      
    </div>
  );
};

export default Input;
